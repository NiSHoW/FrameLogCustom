using System;

namespace FrameLog.Translation.Serializers
{
    public interface ISerializationManager
    {
        string Serialize(object obj);
        Boolean Compare(object oldObject, object newObject);
    }
}
